apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aegis-flow.fullname" . }}-test-connection"
  labels:
    {{- include "aegis-flow.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aegis-flow.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
