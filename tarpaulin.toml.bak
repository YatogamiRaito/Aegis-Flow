[tarpaulin]
# Exclude main.rs from coverage (entry point, hard to unit test)
exclude-files = [
    "crates/proxy/src/main.rs"
]

# Engine (use Auto for compatibility)
engine = "Auto"

# Timeout for long-running tests
timeout = "120s"

# Output settings
output-dir = "coverage"
